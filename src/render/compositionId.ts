/**
 * The registered composition's id, in a plain `.ts` module so the main process
 * (which renders but never compiles JSX) can name the composition without
 * importing the React root.
 */
export const COMMERCIAL_COMPOSITION_ID = 'sowyvid-commercial'
